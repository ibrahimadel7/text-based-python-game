# Inventory pseudocode:
# - Use a dictionary to store items and their counts
# - Add: If item exists, increment count; else, set count to 1
# - Remove: If item exists, decrement count; if count is 0, remove item
# - Display: Print each item and its count
